package selenium;

public class DriverElementscommands {

	public static void main(String[] args) {
		
		/*
		a. get()
		Loads specified URL in current browser window using HTTP POST; blocks until page fully loads (default strategy).​

		b. getCurrentURL()
		Returns String of current page's URL after navigation or page load.​

		c. getPageSource()
		Returns complete HTML source code String of currently loaded page.​

		d. getTitle()
		Returns String of page title from browser tab/head tag.​

		e. close() vs quit()
		close() closes current window/tab only; quit() terminates all browser windows/sessions.​

		f. getWindowHandle()
		Returns unique String identifier of current/active browser window.​

		g. getWindowHandles()
		Returns Set<String> of all open browser window identifiers.​

		h. navigate()
		Provides Navigation interface for to(), back(), forward(), refresh() operations.​

		i. switchTo()
		Switches WebDriver focus to specific frame, window, or alert context.
		
		*/
	}

}
