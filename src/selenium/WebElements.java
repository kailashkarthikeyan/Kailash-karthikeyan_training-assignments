package selenium;

public class WebElements {

	public static void main(String[] args) {
		/*
		a. sendKeys()
		Sends keystrokes or text input to form fields like textboxes.​

		b. clear()
		Removes all existing text content from input fields.​

		c. click()
		Performs mouse click action on clickable elements like buttons/links.​

		d. findElement()
		Locates and returns single WebElement using specified locator strategy.​

		e. findElements()
		Returns List of all WebElements matching the locator (empty if none).​

		f. getDomAttribute()
		Retrieves specified DOM attribute value (e.g., "value", "class", "id").​

		g. getScreenshotAs()
		Captures screenshot of specific WebElement as OutputType (File, Base64).​

		h. getText()
		Returns visible text content of element (inner text).​

		i. isDisplayed()
		Returns true if element visible on page (not hidden).​

		j. isEnabled()
		Returns true if element interactive/enabled (not disabled).​

		k. isSelected()
		Returns true if checkbox/radio/option currently selected.
		
		*/
	}

}
